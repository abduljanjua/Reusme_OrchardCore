 <div class="collapse navbar-collapse" id="navbarSupportedContent">
   <ul class="navbar-nav">
        {% for item in Model.Items %}
        {{ item | shape_render }}
        {% endfor %}
    </ul>
</div>